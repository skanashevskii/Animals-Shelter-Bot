package ru.devpro.enums;

import java.util.HashMap;
import java.util.Map;

public class StateManager {
   /* private final Map<Long, State> userStateMap = new HashMap<>();

    public State getState(Long chatId) {
        return userStateMap.getOrDefault(chatId, State.DEFAULT);
    }

    public void setState(Long chatId, State state) {
        userStateMap.put(chatId, state);
    }*/
}
